import React from 'react';
import styled from 'styled-components';
import { TreeItemProps } from '../atoms/Tree';

const CategoryItem: React.FC<TreeItemProps> = ({
  node: { name, postCount },
  linkProps,
}) => {};

export default CategoryItem;
